<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ══════════════════════════════════════════════════════════════
         SPEDIZIONI — List + Form (standalone)
         ══════════════════════════════════════════════════════════════ -->

    <record id="cf_shipment_list" model="ir.ui.view">
        <field name="name">cf.project.shipment.list</field>
        <field name="model">cf.project.shipment</field>
        <field name="arch" type="xml">
            <list string="Spedizioni"
                  decoration-info="state == 'shipped'"
                  decoration-success="state == 'delivered'"
                  decoration-muted="state == 'draft'">
                <field name="project_id"/>
                <field name="partner_id"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'shipped'"
                       decoration-success="state in ('delivered', 'feedback')"
                       decoration-warning="state == 'ready'"
                       decoration-muted="state == 'draft'"/>
                <field name="carrier"/>
                <field name="tracking_number"/>
                <field name="ship_date"/>
                <field name="estimated_delivery" widget="remaining_days"/>
            </list>
        </field>
    </record>

    <record id="cf_shipment_form" model="ir.ui.view">
        <field name="name">cf.project.shipment.form</field>
        <field name="model">cf.project.shipment</field>
        <field name="arch" type="xml">
            <form string="Spedizione Campioni">
                <header>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,ready,shipped,delivered,feedback"/>
                </header>
                <sheet>
                    <group>
                        <group string="Spedizione">
                            <field name="project_id"/>
                            <field name="partner_id"/>
                            <field name="carrier"/>
                            <field name="tracking_number"/>
                            <field name="tracking_url" widget="url"/>
                        </group>
                        <group string="Date e dettagli">
                            <field name="ship_date"/>
                            <field name="estimated_delivery"/>
                            <field name="actual_delivery"/>
                            <field name="weight"/>
                            <field name="shipping_cost"/>
                        </group>
                    </group>
                    <group string="Prodotti campionati">
                        <field name="product_ids" widget="many2many_tags" nolabel="1"/>
                    </group>
                    <group string="Note">
                        <field name="notes" nolabel="1"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_cf_shipment" model="ir.actions.act_window">
        <field name="name">Spedizioni</field>
        <field name="res_model">cf.project.shipment</field>
        <field name="view_mode">list,form</field>
    </record>
    <record id="action_cf_shipment_view_list" model="ir.actions.act_window.view">
        <field name="sequence">1</field>
        <field name="view_mode">list</field>
        <field name="view_id" ref="cf_shipment_list"/>
        <field name="act_window_id" ref="action_cf_shipment"/>
    </record>
    <record id="action_cf_shipment_view_form" model="ir.actions.act_window.view">
        <field name="sequence">2</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="cf_shipment_form"/>
        <field name="act_window_id" ref="action_cf_shipment"/>
    </record>

</odoo>
