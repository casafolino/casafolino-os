# Deploy casafolino_mail V15 + V16 — 25 aprile 2026

## V15 (18.0.15.0.0) — Brief 3.A: Mass Actions + Scroll Fix + Cleanup

**Commit**: `5cd1356` (feat/mail-mass-action)
**Deploy prod**: 25 aprile 2026, pomeriggio

### Contenuto
- Mass action toolbar su thread list (mark read, archive, trash, move, dismiss senders, permanent delete)
- Undo toast con countdown 10s per tutte le mass action
- Scroll fix thread list (infinite scroll)
- Cron cleanup orphan: pulizia cron duplicati e orfani
- Fix jsonb cast in migration SQL (name è jsonb, code è text)

### Cron registrati
- `_cron_cleanup_expired_logs` (mass action log, ogni 30 min)

---

## V16 (18.0.16.0.0) — Brief 3.B+: Multi-utente Hardening + Mass Action su Search

**Commit**: `56cda42` (feat/mail-mass-action)
**Deploy prod**: 25 aprile 2026, ore ~21:45 CEST

### Contenuto

#### Record rules (22 totali, 11 modelli × user/admin)
| Modello | Domain user | Domain admin |
|---------|------------|-------------|
| casafolino.mail.account | `responsible_user_id = user.id` | `(1,=,1)` |
| casafolino.mail.message | `account_id.responsible_user_id = user.id` | `(1,=,1)` |
| casafolino.mail.tracking | `account_id.responsible_user_id = user.id` | `(1,=,1)` |
| casafolino.mail.thread | `account_id.responsible_user_id = user.id` | `(1,=,1)` |
| casafolino.mail.raw | `account_id.responsible_user_id = user.id` | `(1,=,1)` |
| casafolino.mail.folder | `account_id.responsible_user_id = user.id` | `(1,=,1)` |
| casafolino.mail.folder.rule | `account_id.responsible_user_id = user.id` | `(1,=,1)` |
| casafolino.mail.sender_preference | `account_id.responsible_user_id = user.id` | `(1,=,1)` |
| casafolino.mail.draft | `user_id = user.id` | `(1,=,1)` |
| casafolino.mail.outbox | `account_id.responsible_user_id = user.id` | `(1,=,1)` |
| casafolino.mail.mass.action.log | `user_id = user.id` | `(1,=,1)` |

Admin (`group_system`) bypassa tutte le regole con `[(1,'=',1)]`.

#### Mass action su search results
- Checkbox individuali + select-all nell'header risultati
- Riusa `selectedThreadIds` → toolbar mass action appare automaticamente
- CSS highlight selezione con brand green

#### Cron hardening
- `.sudo()` difensivo su `_cron_fetch_all_accounts`, `_cron_triage_raw`, `_cron_cleanup_raw`

#### Migration 18.0.16.0.0
- No-op su prod: utenti Josefina/Martina già esistenti, account già linkati
- Idempotente: verifica prima di creare/linkare

#### Test file
- `tests/test_multi_user_isolation.py` — 7 test (isolamento, cross-access denial, admin bypass, cron access)

---

## Stato finale prod

| Dato | Valore |
|------|--------|
| Versione modulo | 18.0.16.0.0 |
| Branch server | feat/mail-mass-action |
| Record rules | 22 |
| Account → User | 3/3 linkati |
| Cron attivi | 82, 85, 110, 111 |
| Backup pre-deploy | `/tmp/backup_prod_pre_v16_1777146100.sql.gz` (80MB) |
| TEST OK | Confermato utente 25/04/2026 |

## Utenti multi-account

| Account | Email | User ID | User Login |
|---------|-------|---------|------------|
| 1 | antonio@casafolino.com | 2 | antonio@casafolino.com |
| 2 | martina.sinopoli@casafolino.com | 8 | martina.sinopoli@casafolino.com |
| 3 | josefina.lazzaro@casafolino.com | 6 | josefina.lazzaro@casafolino.com |

## Rollback

```bash
# Restore DB
zcat /tmp/backup_prod_pre_v16_1777146100.sql.gz | docker exec -i -e PGPASSWORD=odoo odoo-app psql -h odoo-db -U odoo folinofood

# Rollback code a V15
cd /home/ubuntu/casafolino-os
git checkout 5cd1356
sudo cp -rf casafolino_mail /docker/enterprise18/addons/custom/
docker exec odoo-app odoo -d folinofood -u casafolino_mail --stop-after-init --no-http
docker restart odoo-app

# Nota: record rules V16 vanno rimosse manualmente se non si fa restore DB
```
